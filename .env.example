# Production: set the real HTTPS backend URL explicitly; do not rely on the localhost fallback.
VITE_API_URL=http://localhost:3000
# Canonical local development mode: management planner reads from backend.
VITE_SERVER_PLANNER_READ=1
# Canonical local development mode: planner mutations persist through backend.
VITE_SERVER_PLANNER_WRITE=1
