VITE_API_URL=http://localhost:3000
# Migration-only preview: load management planner data from backend and keep the UI read-only.
VITE_SERVER_PLANNER_READ=0
# Pilot: server-backed write for individual schedule cells. Implies server read mode.\nVITE_SERVER_PLANNER_WRITE=0\n